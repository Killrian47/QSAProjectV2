<header>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light d-md-none">
            <div class="container-fluid">
                <div class="d-flex justify-content-center">
                    <img src="{{ asset('assets/images/LOGO-QSA-CONSEIL.png') }}" class="qsa-logo" alt="Logo QSA">
                </div>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{ path('app_home') }}">Toutes vos analyses / Accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_order') }}">Envoyer de nouveaux échantillons</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_account') }}">Mon compte</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
        <div class="d-none d-md-flex justify-content-center">
            <img src="{{ asset('assets/images/LOGO-QSA-CONSEIL.png') }}" class="qsa-logo" alt="Logo QSA">
        </div>
        <div class="d-none d-md-block qsa-nav-bar mt-2">
            {% if app.user %}
                <ul class="d-flex justify-content-center">
                    <li class="p-2 qsa-link"><a href="{{ path('app_home') }}" class=" qsa-link-header">Toutes vos analyses / Accueil </a></li>
                    <li class="p-2 qsa-link"><a href="{{ path('app_order') }}" class=" qsa-link-header">Envoyer de nouvelles analyse</a></li>
                    <li class="p-2 qsa-link"><a href="{{ path('app_account') }}" class=" qsa-link-header">Mon compte</a></li>
                    <li class="p-2 qsa-link"><a href="{{ path('app_logout') }}" class=" qsa-link-header">Se déconnecter</a></li>
                </ul>

            {% endif %}
        </div>

    </header>
</header>