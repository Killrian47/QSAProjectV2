{% if entreprisesStartByF is defined %}
    {% if entreprisesStartByF is not empty %}
        <div>
            <h2>F</h2>
            <hr class="sep w-50">
            <div class="list-entreprise">
                {% for entreprise in entreprisesStartByF %}
                    {% include 'admin/entreprise/entreprise.html.twig' %}

                {% endfor %}
            </div>
        </div>
    {% else %}
        <div>
            <h2>F</h2>
            <hr class="sep w-50">
            <div class="d-flex justify-content-center align-items-center">
                <p>Pas d'entreprise commencant par F</p>
            </div>
        </div>
    {% endif %}
{% endif %}